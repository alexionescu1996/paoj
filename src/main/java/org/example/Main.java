package org.example;

public class Main {

    public static void main(String[] args) {
        // see individual packages per topic:
        // - inheritance   → Hero implements Flyable, Shootable, Invisible
        // - extensions    → SuperVillain extends DangerousVillain, Immortal
        // - anonymous     → Greeter via anonymous class
        // - comparable    → Hero implements Comparable, sorted by powerLevel
        // - comparator    → Hero sorted by powerLevel or name via Comparator
        // - callback      → OnMissionComplete callback passed to Mission
    }
}
